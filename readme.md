📷 ระบบบันทึกเวลาเข้างานด้วยใบหน้าและ GPS (Face Recognition Attendance with GPS)
เว็บแอปพลิเคชันสำหรับบันทึกเวลาเข้า-ออกงาน โดยใช้เทคโนโลยี จดจำใบหน้า (Face Recognition) ผ่านไลบรารี face-api.js ร่วมกับระบบ ตรวจสอบพิกัด (GPS Geofencing) ทำงานแบบ Serverless บน Google Apps Script และเก็บข้อมูลทั้งหมดลงใน Google Sheets โดยตรง

✨ ฟีเจอร์หลัก (Key Features)
Serverless 100%: ไม่ต้องเช่าโฮสติ้ง รันบน Google Cloud (Apps Script) ฟรี

AI จดจำใบหน้า: ใช้ face-api.js ตรวจจับและเปรียบเทียบใบหน้าผ่านเบราว์เซอร์ แม่นยำด้วยค่าความเหมือน (Confidence Score)

จำกัดพื้นที่ลงเวลา (GPS Geofencing): ระบบจะอนุญาตให้ลงเวลาได้เฉพาะเมื่ออยู่ในรัศมีที่กำหนด (เช่น 100 เมตรจากออฟฟิศ)

Database Real-time: สร้างและบันทึกข้อมูลลง Google Sheets อัตโนมัติ (แยกแผ่นงาน Users, Attendance, Config)

ใช้งานง่าย:

ลงทะเบียน: ถ่ายรูปเก็บใบหน้าพนักงานได้เองผ่านหน้าเว็บ

สแกนเข้างาน: รวดเร็ว มีระบบยืนยันตัวตนพร้อมแสดงรูปถ่ายและคะแนนความเหมือน

ตั้งค่า: มีหน้า Admin สำหรับตั้งพิกัด GPS ของสถานที่ทำงาน

รองรับมือถือ: ออกแบบหน้าตา (UI) ให้สวยงามและใช้งานบนมือถือได้สะดวก (ใช้ฟอนต์ Sarabun)

🛠️ เทคโนโลยีที่ใช้ (Tech Stack)
Frontend: HTML5, CSS3, JavaScript

AI Library: face-api.js (โหลดผ่าน CDN)

Backend: Google Apps Script (.gs)

Database: Google Sheets

🚀 วิธีติดตั้งและใช้งาน (Installation)
เนื่องจากโปรเจกต์นี้รันบน Google Apps Script คุณไม่จำเป็นต้องมีเซิร์ฟเวอร์ เพียงทำตามขั้นตอนนี้:

1. สร้าง Google Sheet
ไปที่ Google Sheets สร้างไฟล์ใหม่

ตั้งชื่อไฟล์ตามต้องการ (เช่น "Attendance System Data")

2. เปิด Apps Script
ในหน้า Google Sheets ไปที่เมนู Extensions (ส่วนขยาย) > Apps Script

ลบโค้ดเดิมในไฟล์ Code.gs ออกให้หมด

3. ลงไฟล์โปรเจกต์
คัดลอกโค้ดจากใน Repository นี้ไปใส่:

code.gs: นำไปวางในไฟล์ Code.gs

สร้างไฟล์ HTML โดยกดเครื่องหมาย (+) > HTML

ตั้งชื่อไฟล์ให้ตรงเป๊ะ: menu, register, scan, config

นำโค้ด HTML ของแต่ละหน้าไปวางให้ตรงกัน

4. Deploy (การทำให้ใช้งานได้)
กดปุ่ม Deploy (ทำให้ใช้งานได้) มุมขวาบน > New deployment (การทำให้ใช้งานได้รายการใหม่)

กดรูปเฟือง ⚙️ เลือก Web app

ตั้งค่าดังนี้:

Description: (ใส่อะไรก็ได้ เช่น v1)

Execute as (ดำเนินการในฐานะ): Me (อีเมลของคุณ)

Who has access (ผู้ที่มีสิทธิ์เข้าถึง): Anyone (ทุกคน) หรือ Anyone with Google account แล้วแต่ความต้องการ

กด Deploy

Authorize Access: ระบบจะขอสิทธิ์เข้าถึง (กล้อง, Sheets) ให้กดอนุญาต (หากขึ้นเตือนความปลอดภัย ให้กด Advanced > Go to (Project Name) (unsafe))

5. รับลิงก์ Web App
คัดลอก Web App URL ที่ได้ เพื่อส่งให้พนักงานใช้งาน

⚙️ คู่มือการใช้งานเบื้องต้น
เมื่อเปิดลิงก์ Web App เข้ามา จะเจอเมนูหลัก:

ตั้งค่าพิกัด (Admin):

เข้าเมนู ⚙️ ตั้งค่าระบบ (Admin)

ไปยืน ณ จุดที่ต้องการให้เป็นจุดเช็คอิน

กดปุ่ม 📍 ดึงตำแหน่งปัจจุบันของฉัน

ระบุรัศมี (Radius) เป็นกิโลเมตร (เช่น 0.05 คือ 50 เมตร)

กดบันทึก (ระบบจะสร้างแผ่นงาน Config ให้เอง)

ลงทะเบียนพนักงาน:

เข้าเมนู 👤 ลงทะเบียนพนักงานใหม่

ใส่ชื่อพนักงาน

กดบันทึกใบหน้า (แนะนำให้บันทึก 3-4 มุมเพื่อความแม่นยำ)

(ระบบจะสร้างแผ่นงาน Users ให้เอง)

สแกนเข้างาน:

เข้าเมนู 📷 สแกนเข้างาน

ระบบจะเช็ค GPS ก่อนว่าอยู่ในพื้นที่หรือไม่

ถ้าผ่าน กล้องจะเปิดขึ้นมา เมื่อสแกนเจอใบหน้าและมั่นใจเกินเกณฑ์ที่ตั้งไว้

จะมีหน้าต่างยืนยันชื่อและเวลา กด ยืนยัน เพื่อบันทึก

(ข้อมูลจะถูกบันทึกลงแผ่นงาน Attendance)

⚠️ ข้อควรระวัง
Permission: เบราว์เซอร์จะขออนุญาตใช้ กล้อง และ ตำแหน่ง (Location) ผู้ใช้ต้องกดอนุญาต

HTTPS: ระบบ Face Recognition และ GPS ต้องรันบน HTTPS เท่านั้น (ซึ่ง Google Apps Script มีให้อยู่แล้ว)

แสงสว่าง: ควรลงทะเบียนและสแกนในที่ที่มีแสงสว่างเพียงพอ

🤝 เครดิต (Credits)
Face Recognition Logic: face-api.js by Vincent Mühler

Font: Sarabun via Google Fonts
